{
  "name": "backend",
  "version": "1.0.0",
  "description": "Shamsi Institute Quiz System Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "init-admin": "node -e \"const Admin = require('./models/Admin'); const admin = new Admin({username:'admin',password:'admin123'}); admin.save().then(()=>console.log('✅ Admin created')).catch(e=>console.log('❌ Error:',e.message))\""
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^8.2.1",
    "json2csv": "^6.0.0-alpha.2",
    "mongoose": "^7.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
